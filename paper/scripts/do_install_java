# update all of the OS packages
$ apt-get update

# Install Java as it is needed by Hadoop, -y informs apt-get to select yes for any options the user might be queried with 
$ apt-get install default-jdk -y

# Ensure Java is installed resulting in output akin to:
$ java --version 

java version "1.7.0_65"
OpenJDK Runtime Environment (IcedTea 2.5.1) (7u65-2.5.1-4ubuntu1~0.14.04.2)
OpenJDK 64-Bit Server VM (build 24.65-b04, mixed mode)